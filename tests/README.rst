====================================================
Build and Run test cases
===================================================

To build Fledge "Threshold" filter plugin unit test cases:

.. code-block:: console

  $ mkdir build
  $ cd build
  $ cmake ..
  $ make
  $ ./RunTests
